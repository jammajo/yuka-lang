// Literales
true;
false;
None;
"Hello, world!";
123;
45.67;

// Agrupaciones
(1 + 2);
("a" + "b");
(3 * (4 + 5));

// Unarios
!false;
-42;
-3.14;

// Variables
x;
_some_var;
Data42;

// Aritmética
1 + 2 - 3 * 4 / 5;
(a + b) * (c - d);

// Comparaciones
a == b;
x != y;
x < 10;
y >= 3.14;

// Lógicos
true && false;
x || y;
!z;

// Acceso a propiedades
foo.bar;
foo.bar.baz;
data.get.value;

// Llamadas
foo();
foo(1, 2, 3);
call(123)(true).prop;
(call)().x;

// Llamada + acceso combinados
object.method().prop;
a.b().c.d();

// Ternario
x > 0 ? "positive" : "non-positive";
condition ? a + b : c - d;

// Mezclas complejas
(obj.method(42)).prop + (nested ? 1 : 0);
